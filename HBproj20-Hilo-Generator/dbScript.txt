1>It work with all DataBase..(Removed from 5.x)
2>It use high and low algorithm to generate id values by using given table and column name..
3>If there is no table then its use(HIBERNATE_UNIQUE_KEY as table and NEXT_HI as default column name...
4>Formula==<high value>*(max_lo param value+1)...
5>If we don't Specify max_lo values in <param >then it take 32767 as default values....
